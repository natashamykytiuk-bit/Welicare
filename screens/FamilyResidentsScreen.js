import PlaceholderScreen from '../components/PlaceholderScreen';

export default function FamilyResidentsScreen({ navigation }) {
  return (
    <PlaceholderScreen
      navigation={navigation}
      title="My Residents"
      description="See the residents your family is connected to and follow their updates."
      settingsTarget="Settings"
    />
  );
}
