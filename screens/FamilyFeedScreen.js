import PlaceholderScreen from '../components/PlaceholderScreen';

// Reached from Caregiver Mode's quick links.
export default function FamilyFeedScreen({ navigation }) {
  return (
    <PlaceholderScreen
      navigation={navigation}
      title="Family Feed"
      description="Share updates, photos, and moments with family members following along."
    />
  );
}
